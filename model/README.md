# Model artifacts

Place the following files here (generated from `01_eda&model.ipynb`):

- `demand_model.joblib` – trained XGBoost demand model
- `model_input_schema.parquet` – scenario table (product_id, date, feature columns) for the dashboard

Run the notebook's "Save model and schema" cell to create them in this folder.
